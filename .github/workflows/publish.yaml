name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@3b781dc75a8218d568ddbd4b92aee971bf9e835d # v3.1.3
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
