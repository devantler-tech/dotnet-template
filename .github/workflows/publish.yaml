name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@4f4e07a3ebf3e1161756a292966e36c91a65ee04 # v13.3.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
