name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@11f70d3be475887db4f262f415fcb19c72a1fa73 # v3.0.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
