name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@8349b0935ade387313cf1a92d17dc068d92daee1 # v13.0.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
