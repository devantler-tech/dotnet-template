name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@41924797ae44fd5cc674e0914db55712da18e68f # v3.2.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
