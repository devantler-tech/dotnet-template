name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@e5eea909adaf782180f325c37a1311661b25bbb7 # v5.6.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
