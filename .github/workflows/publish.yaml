name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@4f9b2bb971efb754b719e8ecb39bdf74b2490c33 # v13.0.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
