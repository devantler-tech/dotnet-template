name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@f7c1091196016d2e66b7487ceba166112d196702 # v1.25.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
