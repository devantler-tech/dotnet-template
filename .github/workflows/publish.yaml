name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@d6c7133cb8cb13189b97993aeb3f4d120f27479d # v5.4.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
