name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@4afd2c6a09ab380e67b2f3f8cde72b67f45bd192 # v2.3.3
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
