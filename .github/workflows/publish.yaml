name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@61d9c87b302523778789c7cf09655b87b6716e87 # v13.0.4
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
