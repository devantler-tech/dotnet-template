name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@9705e47d7df0b556ec06786247297c0e5020e56d # v10.2.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
