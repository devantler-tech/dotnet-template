name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@5e32b7d4ff76d3de0c8cfdab4942fe304dd185a9 # v5.5.3
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
