name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@d5220ca536bd22c2e3ffe69b243a83fdf0beea03 # v1.29.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
