name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@7d3a08e3cc284c6fa14a056d5df7702ec0978d59 # v5.5.10
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
