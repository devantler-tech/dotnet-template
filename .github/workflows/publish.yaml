name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@061b345a7351b0caf23055caea63ccd08b75e20e # v8.0.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
