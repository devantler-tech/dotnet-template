name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@ee4948f9372d00126a24ce30db288294b86568d1 # v13.10.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
