name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@b47999a5a26515e23d09dbc5bdc62a5e59343f9e # v10.2.6
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
