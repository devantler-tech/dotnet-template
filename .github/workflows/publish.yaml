name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@4e94f19d983240a1ae8d4aa3f06356fa91c0ff33 # v13.7.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
