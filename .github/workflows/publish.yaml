name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@b69839ba2debe04a46321b7698b6daf57667d4e7 # v9.0.2
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
