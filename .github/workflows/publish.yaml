name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@6f47d8ca360fa3058f57175d54fa148deac08983 # v13.6.6
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
