name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@26f77bf515895dc8b750130ea4625a9fd8648c9c # v3.1.4
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
