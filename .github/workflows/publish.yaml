name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@883d891a0e6a2c9420d2b60aea9d5f47c20ce803 # v13.5.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
