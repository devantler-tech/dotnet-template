name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@d72ecd5e8b680c2066a490a2b761a8913c454575 # v13.1.3
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
