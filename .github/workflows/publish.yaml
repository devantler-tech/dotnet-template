name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@0f7b26323a6983141526673207d381445851aec3 # v1.29.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
