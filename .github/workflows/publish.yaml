name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@3ee6cb9bbd616c2a38779be41a5e41f3e77282d0 # v13.2.3
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
