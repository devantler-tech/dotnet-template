name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@5aa2657f976103d716f585266a0fa4e964594df2 # v9.0.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
