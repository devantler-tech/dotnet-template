name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@6ed1f44e8f10e4a87746e99159e30b215b9fe8e6 # v11.0.2
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
