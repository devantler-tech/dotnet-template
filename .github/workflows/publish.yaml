name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@6b44cfb837e545a8179b0cc97a63b7c694cb9287 # v5.5.7
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
