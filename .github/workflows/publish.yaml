name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@a17d133042df01dbfeab0e22df6f5c95fc86bd82 # v5.6.5
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
