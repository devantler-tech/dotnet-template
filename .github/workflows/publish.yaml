name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@e5d0f20a87d50518123513d07d633c243af6c1e6 # v5.5.8
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
