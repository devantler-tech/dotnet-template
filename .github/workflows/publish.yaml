name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@cf66636f30170e30cc00b19ddc54b4101b4e8516 # v13.0.5
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
