name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@c46f75962deb2dab6619de8a379b47b929f40d15 # v2.3.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
