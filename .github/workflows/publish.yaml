name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@b748f68a0d14ad477cb9610a6d1f958bf75e91dc # v5.2.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
