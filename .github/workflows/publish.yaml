name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@b3783a5148b844b3f1f4011cdc84871ad66348c1 # v13.0.9
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
