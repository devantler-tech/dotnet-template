name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@a147ead8b8bc937fbfb1974e3698c7e32f95517d # v13.5.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
