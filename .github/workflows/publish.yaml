name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@0f27c13a9785608e0bbf9998777d27761b4de617 # v10.0.2
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
