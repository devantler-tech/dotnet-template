name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@a286df5616b1aa89a0136d0eed82b462dbac7585 # v13.0.7
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
