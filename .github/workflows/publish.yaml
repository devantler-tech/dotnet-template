name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@c38023c534dd7c47e32d41b28a2ce6102359d9de # v3.0.2
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
