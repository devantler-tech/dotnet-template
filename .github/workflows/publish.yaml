name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@d4d382168eeef3d08606ed7c6fb2671e3d4170fc # v5.5.2
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
