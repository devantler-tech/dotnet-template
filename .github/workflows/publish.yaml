name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@8ff376e5da80e2a489b9bf055ab518fcbad8aa0a # v13.1.8
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
