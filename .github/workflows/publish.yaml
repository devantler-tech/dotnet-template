name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@b089a1b041cb86af22cdc57de58a4d7d258dcc32 # v13.1.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
