name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@e0863b182654aaca93f9a94599f04f3433a5ff5a # v1.31.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
