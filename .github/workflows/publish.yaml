name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@3c3a6348b3e7aacc995ae49fc60032f95f70a779 # v13.6.5
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
