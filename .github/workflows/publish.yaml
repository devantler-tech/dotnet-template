name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@5de934e076503c897d43d85680406f8d1d9356b7 # v10.0.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
