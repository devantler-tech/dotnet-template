name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@0763de30f9598601916680b2f52c9f41f14082df # v1.21.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
