name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@84813ae61fb21741e0a6193b5c14c6941a448577 # v9.0.7
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
