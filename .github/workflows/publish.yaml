name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@931b82faa0c30e65f626527a6b3c8cd2d4879c71 # v9.0.11
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
