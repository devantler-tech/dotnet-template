name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@984aa9d877ef8618ec7c29cf3db7d7aa26931d5a # v3.1.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
