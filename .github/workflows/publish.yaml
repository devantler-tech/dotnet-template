name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@759ff9b2526cd1b142b49c5dc1616118a1ef44d9 # v13.6.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
