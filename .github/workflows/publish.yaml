name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@ad99c16f59e9e205d1cbd4d511c949aa353670b7 # v5.0.0
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
