name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@efdff74933a4c137d5b442f54687882a24de88db # v13.6.3
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
