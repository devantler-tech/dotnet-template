name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/actions/.github/workflows/publish-dotnet-library.yaml@0de1d284f879d62d6fe316d3997fd3ea8f092fb3 # v13.2.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
