name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/publish-dotnet-library.yaml@f974f62a8cb396130fa084d209ad1fc17e99b9fc # v5.6.1
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
