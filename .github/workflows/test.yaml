name: Test .NET solution or project
permissions:
  packages: read
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    uses: devantler-tech/reusable-workflows/.github/workflows/ci-dotnet-test.yaml@14ced1780351f91e8ebdc5324cf7590ebebf8a66 # v1.19.8
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
