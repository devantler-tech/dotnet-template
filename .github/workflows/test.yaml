name: Test .NET solution or project
permissions:
  packages: read
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    uses: devantler-tech/reusable-workflows/.github/workflows/ci-dotnet-test.yaml@0f7b26323a6983141526673207d381445851aec3 # v1.29.1
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
