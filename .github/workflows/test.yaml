name: Test .NET solution or project
permissions:
  packages: read
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    uses: devantler-tech/reusable-workflows/.github/workflows/ci-dotnet-test.yaml@e0863b182654aaca93f9a94599f04f3433a5ff5a # v1.31.1
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
