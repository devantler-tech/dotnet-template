name: Test .NET solution or project
permissions:
  packages: read
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    uses: devantler-tech/reusable-workflows/.github/workflows/run-dotnet-tests.yaml@4afd2c6a09ab380e67b2f3f8cde72b67f45bd192 # v2.3.3
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
