name: Test .NET solution or project
permissions:
  packages: read
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    uses: devantler-tech/reusable-workflows/.github/workflows/ci-dotnet-test.yaml@d5220ca536bd22c2e3ffe69b243a83fdf0beea03 # v1.29.0
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
